spring:
  datasource:
    hikari:
      max-lifetime: 10000
      connection-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
